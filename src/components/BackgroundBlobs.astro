<div class="bg-blobs" aria-hidden="true">
  <span class="bg-blob bg-blob--one"></span>
  <span class="bg-blob bg-blob--two"></span>
  <span class="bg-blob bg-blob--three"></span>
</div>

<style>
  .bg-blobs {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .bg-blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(clamp(38px, 5.5vw, 64px));
    will-change: transform;
  }

  .bg-blob--one {
    top: -8%;
    left: -6%;
    width: clamp(240px, 56vw, 500px);
    height: clamp(240px, 56vw, 500px);
    background: radial-gradient(circle, rgba(90, 122, 82, 0.3) 0%, rgba(90, 122, 82, 0.06) 45%, transparent 72%);
    animation: blob-drift-one 30s ease-in-out infinite;
  }

  .bg-blob--two {
    top: 40%;
    right: -10%;
    width: min(62vw, 560px);
    height: min(62vw, 560px);
    background: radial-gradient(circle, rgba(139, 99, 71, 0.22) 0%, rgba(139, 99, 71, 0.05) 45%, transparent 72%);
    animation: blob-drift-two 36s ease-in-out infinite;
  }

  .bg-blob--three {
    bottom: -12%;
    left: 28%;
    width: clamp(220px, 52vw, 460px);
    height: clamp(220px, 52vw, 460px);
    background: radial-gradient(circle, rgba(143, 168, 130, 0.28) 0%, rgba(143, 168, 130, 0.05) 45%, transparent 72%);
    animation: blob-drift-three 33s ease-in-out infinite;
  }

  @keyframes blob-drift-one {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(16vw, 20vh) scale(1.08); }
    66% { transform: translate(6vw, 44vh) scale(0.94); }
  }

  @keyframes blob-drift-two {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-20vw, -16vh) scale(1.06); }
    66% { transform: translate(-10vw, 12vh) scale(0.92); }
  }

  @keyframes blob-drift-three {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-14vw, -26vh) scale(1.1); }
    66% { transform: translate(12vw, -10vh) scale(0.96); }
  }

  @media (prefers-reduced-motion: reduce) {
    .bg-blob { animation: none; }
  }
</style>
